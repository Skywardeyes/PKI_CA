---
description: 每次功能性更改必须同步更新文档
alwaysApply: true
---

# 文档同步规则

当变更涉及功能、接口、脚本行为、执行步骤、验证方式时，必须同步更新文档。

## 必须执行

- 在实现完成后，检查是否需要更新 `README.md`、`docs/02-执行手册.md`、`web/README.md`（按实际影响范围选择）。
- 若新增/修改了命令、按钮、API、参数、默认端口、目录结构，文档中必须出现一致描述。
- 提交前确认文档与代码不矛盾：路径、命令、参数名、步骤顺序必须可直接执行。

## 禁止行为

- 只改代码不改文档（当代码语义已变化时）。
- 在文档中保留已删除功能或旧命令。

## 通过标准

- 变更说明能在文档中找到对应位置。
- 新用户仅按文档即可完成演示或复现。
